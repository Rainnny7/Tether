type TetherConfig = {
    /**
     * The API endpoint to connect to.
     */
    endpoint?: string;

    /**
     * Whether the connection should be secure.
     */
    secure?: boolean;
};
export default TetherConfig;
